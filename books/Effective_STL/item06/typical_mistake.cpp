#include <Widget.h>

int main()
{
    // This doesn't declare a Widget w1, it declares a function named w1 that
    // takes nothing and returns a Widget.
    Widget w1();

    // This does declare a Widget w2.
    Widget w2;
}
